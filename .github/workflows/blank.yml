name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello
        run: echo "First Step on first Job"
      - name: Print Second
        run: echo "Second Step Printing"
